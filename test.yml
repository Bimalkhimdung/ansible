---
- name: Building BE 
  hosts: build-server
  gather_facts: false
  vars:
      work_dir: "/home/build/aayulogic"
      be_dir: "{{ work_dir }}/irealhrsoft-backend"
      python_dir: "{{ be_dir }}/realhrsoft/bin/python"
      git_dir: "/usr/bin/git"

  tasks:
      - name: Get current date
        command: date +"%d%m%Y"
        register: current_date
      - name:  make dir
        command: "mkdir test-awx2 chdir=/home/build"

        

       
       
       
  